---
import type { RichTextField, ImageField } from "@prismicio/client";
import PrismicRichText from "./PrismicRichText.astro";

interface Props {
  title: RichTextField;
  text: RichTextField;
  backgroundImage: ImageField;
  ctaText: string;
}

const { title, text, backgroundImage, ctaText } = Astro.props;
---

<section
  class="min-h-[calc(100vh-72px)] flex items-center bg-[#7ea9c4] relative"
>
  <div class="absolute h-full w-full inset-0">
    <!-- <div class="absolute h-full w-full inset-0 bg-cyan-200/30"></div> -->
    {
      backgroundImage.url && (
        <img
          src={backgroundImage.url}
          alt={backgroundImage.alt ?? ""}
          aria-hidden="true"
          class="w-full h-full object-cover object-center opacity-50"
        />
      )
    }
  </div>

  <div
    class="w-full h-full max-w-5xl mx-auto flex flex-col gap-6 items-start justify-center px-14 py-20 relative"
  >
    <PrismicRichText
      field={title}
      class="text-3xl xl:text-4xl font-serif text-[#1E1816]"
    />
    <PrismicRichText field={text} class="text-xl text-[#4A3E36]" />

    <a
      href="/#servicios"
      style="box-shadow: rgba(255, 255, 255, 0.55) -1px -1px 0.5px 0px, rgba(255, 255, 255, 0.25) 1px 1px 0.5px 0px;"
      class="mt-10 text-neutral-600 cursor-pointer overflow-hidden transition-all group px-4 py-2 group backdrop-blur-xs font-semibold rounded-full hover:scale-101 duration-500 group bg-linear-to-tl to-neutral-50/10 from-neutral-800/10 bg-transparent relative"
    >
      {ctaText}
      <span class="text-base ml-2">→</span>
    </a>
  </div>
</section>
